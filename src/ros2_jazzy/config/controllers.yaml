controller_manager:
  ros__parameters:
    update_rate: 100

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    # leg_controller:
    #   type: effort_controllers/JointGroupEffortController

leg_controller:
  ros__parameters:
    type: effort_controllers/JointGroupEffortController
    joints: ['hip','osl_hip','knee', 'osl_knee', 'ankle','osl_ankle']
